// var connection = {
// };

// var wp = new WordPress(connection.url, connection.username, connection.password);
// // var blogId = 1;
// // var postId = 1858;
// // var object = wp.getPost(blogId, postId);

// // console.log(object.post_content)

// var posts = wp.getPosts(1, {post_type:"post"});

// console.log(posts);